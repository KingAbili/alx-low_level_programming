#include "main.h"
#include <stdio.h>

/**
 * main - entry, what's my name
 * @argc: count
 * @argv: vector
 * Return: 0 successful
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
